import ApperIcon from '../ApperIcon' // Assuming ApperIcon is in src/components/

const AppIcon = ({ name, size, className }) => {
  return <ApperIcon name={name} size={size} className={className} />
}

export default AppIcon